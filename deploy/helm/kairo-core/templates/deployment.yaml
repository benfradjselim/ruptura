apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kairo-core.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kairo-core.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  # Badger (embedded KV) is single-writer; Recreate avoids two pods fighting over the PVC.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "kairo-core.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kairo-core.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "kairo-core.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: kairo-core
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          args:
            - "--port"
            - "8080"
            - "--storage"
            - "/var/lib/kairo/data"
            # $(KAIRO_API_KEY) is expanded by kubelet from the env var below.
            - "--api-key"
            - "$(KAIRO_API_KEY)"
          env:
            - name: KAIRO_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "kairo-core.secretName" . }}
                  key: api-key
                  optional: true
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/kairo/data
            # distroless nonroot needs a writable /tmp for Badger temp files
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "kairo-core.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
