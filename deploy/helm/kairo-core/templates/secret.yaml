{{- if and (not .Values.existingSecret) .Values.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kairo-core.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kairo-core.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.apiKey | quote }}
{{- end }}
