apiVersion: apps/v1
kind: Deployment
metadata:
  name: processor
  namespace: {{ .Values.namespace }}
  labels:
    app: processor
    app.kubernetes.io/name: mlops-anomaly-detection
spec:
  replicas: {{ .Values.services.processor.replicas }}
  selector:
    matchLabels:
      app: processor
  template:
    metadata:
      labels:
        app: processor
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      automountServiceAccountToken: false
      containers:
        - name: processor
          image: {{ .Values.image.repository }}-processor:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          ports:
            - containerPort: {{ .Values.services.processor.port }}
          envFrom:
            - configMapRef:
                name: mlops-config
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            {{- toYaml .Values.services.processor.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.processor.port }}
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.processor.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.pvc.name }}
