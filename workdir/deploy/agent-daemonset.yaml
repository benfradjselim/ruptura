---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ruptura-agent
  namespace: ruptura-system
  labels:
    app.kubernetes.io/name: ruptura
    app.kubernetes.io/component: agent
    app.kubernetes.io/version: "4.0.0"
spec:
  selector:
    matchLabels:
      app: ruptura-agent
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: ruptura-agent
        app.kubernetes.io/name: ruptura
        app.kubernetes.io/component: agent
    spec:
      serviceAccountName: ruptura-agent
      hostPID: true          # needed to collect host process metrics
      hostNetwork: false
      dnsPolicy: ClusterFirstWithHostNet
      tolerations:
        - operator: Exists   # schedule on all nodes including taints
      securityContext:
        runAsNonRoot: false  # agent needs host metrics access
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ruptura-agent
          image: docker.io/library/ruptura:latest
          imagePullPolicy: Never
          command: ["/ruptura", "agent"]
          args:
            - --central-url=http://ruptura.ruptura-system.svc.cluster.local
            - --interval=15s
          env:
            - name: RUPTURA_CENTRAL_URL
              value: "http://10.43.59.23"   # ClusterIP fallback when CoreDNS unavailable
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /api/v2/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v2/ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          securityContext:
            runAsUser: 0             # needs root for /proc inspection
            privileged: false
            capabilities:
              add: ["SYS_PTRACE"]   # needed for /proc inspection
              drop: ["ALL"]
          volumeMounts:
            - name: proc
              mountPath: /host/proc
              readOnly: true
            - name: sys
              mountPath: /host/sys
              readOnly: true
            - name: rootfs
              mountPath: /rootfs
              readOnly: true
            - name: ruptura-data
              mountPath: /var/lib/ruptura
      volumes:
        - name: proc
          hostPath:
            path: /proc
        - name: sys
          hostPath:
            path: /sys
        - name: rootfs
          hostPath:
            path: /
        - name: ruptura-data
          emptyDir: {}
